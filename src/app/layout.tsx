import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ViralFaceless AI - Create Viral Faceless Content with AI",
  description: "AI-powered platform to create viral faceless content in minutes. Generate ideas, scripts, voice-overs, videos, thumbnails, and SEO-optimized metadata.",
  keywords: ["ViralFaceless", "AI", "faceless content", "viral videos", "content creation", "TikTok", "YouTube Shorts", "Instagram Reels"],
  authors: [{ name: "ViralFaceless AI" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster richColors position="top-right" />
      </body>
    </html>
  );
}
